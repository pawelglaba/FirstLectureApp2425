package com.example.firstapplecture

/**
 * Gender is an enumeration representing the gender options available to users.
 * It includes MALE, FEMALE, and PREFER_NOT_TO_SAY.
 */

enum class Gender {
    MALE, FEMALE, PREFER_NOT_TO_SAY
}